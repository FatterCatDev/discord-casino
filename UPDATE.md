# Pending Update

version: 1.0.6

## Changes
- Emoji usage is centralized via `src/lib/emojis.mjs`, swapping in the casino’s custom emoji set across commands while keeping race-track code blocks on vanilla Unicode icons.
