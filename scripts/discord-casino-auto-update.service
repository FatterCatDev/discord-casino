[Unit]
Description=Discord Casino auto-update watcher
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
# Adjust if the repository lives somewhere else.
WorkingDirectory=%h/discord-casino
ExecStart=/usr/bin/env bash %h/discord-casino/scripts/auto-update.sh
Environment=PATH=/usr/local/bin:/usr/bin:/bin
# Optional user overrides live in ~/.config/discord-casino/auto-update.env
EnvironmentFile=-%h/.config/discord-casino/auto-update.env
Restart=always
RestartSec=5

[Install]
WantedBy=default.target
